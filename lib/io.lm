(define println 
  (lambda (x) 
    (begin 
      (print x)
      (print "\n")))) 

(define print-stream
  (lambda (s)
    (begin
      (print "(")
      (if (not (empty-stream? s))
          (begin
            (for-each-stream
              (lambda (x) (begin (print x) (print " ")))
              (init-stream s))
            (print (last-stream s)))
          nil)
      (print ")"))))

(define println-stream
  (lambda (s)
    (begin
      (print-stream s)
      (print "\n"))))

