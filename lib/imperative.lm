(defmacro (while test body)
  (if (eval test)
      (begin
        (eval body)
        (while test body))
      nil))

(defmacro (for i a b body)
  (local
    (define i (eval a))
    (define b' (eval b))
    (while (<= i b')
      (begin
        (eval body)
        (inc! i)))))
